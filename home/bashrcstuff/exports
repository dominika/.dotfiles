source "$DOT_FILES/configs/private"

export MANPAGER='col -bx | vim -c ":set ft=man nonu nolist" -R -'
export LANG=en_US.UTF-8
export LC_ALL=$LANG
export LS_OPTIONS='--color=auto'
export EDITOR='vim'
export GEM_EDITOR='vim'
#export CC=/usr/bin/gcc-4.2

export OPSCODE_USER=maui

export RAILS_ENV=development

export PATH="/usr/local/bin:/usr/local/sbin:/usr/local/heroku/bin:/usr/local/opt/coreutils/libexec/gnubin:$PATH"
#export JAVA_HOME="/System/Library/Frameworks/JavaVM.framework/Versions/1.6/Home"
#export PATH="/System/Library/Frameworks/JavaVM.framework/Versions/1.6/Home/bin:$PATH"

# because nvm is SO slow, needs to be change on node version upgrade
#export NVM_DIR="$HOME/.nvm"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm ]
export PATH=~/.nvm/versions/node/v5.10.1/bin:$PATH

eval `dircolors ~/.dir_colors`

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
